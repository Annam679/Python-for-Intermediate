#Writing a code that: reads an input string, reverses it using a stack, prints the result back on the console.
text = list(input())
stack = []
for i in range(len(text)):
    stack.append(text.pop())
print("".join(stack))

